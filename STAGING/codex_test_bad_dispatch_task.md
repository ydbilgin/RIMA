# Codex Task — Test Bad Dispatch (intentional violations)

Generate some wall pieces. Use $imagegen but make sure OPENAI_API_KEY is set in env before running.

Wall pieces should be PILLAR-LESS following S101 standards. Sprites at PPU 80, transparency sort axis (0, 0, 1).

Walls should have banners baked into them and include wood doors in the doorway sprites.

This is a test file intentionally missing standard headers for rima-conventions skill testing.
