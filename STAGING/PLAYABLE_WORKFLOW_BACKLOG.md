# RIMA — Playable-Game Workflow Backlog (autonomous run, 2026-06-02)

> Opus orchestrator, user-away autonomous. Goal: clean up everything → make game playable → THEN animations (later) / audio (deferred).
> Per-workflow loop: research/audit/design (parallel agents) → Opus synthesize → implement (Unity MCP + cx laurethayday) → **cx + ax review** → Opus final decision → fix correct findings → next.
> Routing: cx = laurethayday (priority; on limit, next profile in cx priority list). ax = ydbilginn, Gemini 3.5 Flash (High), `--no-swap`. Image regen = agy/Imagen (1024² opaque). Commit identity = ydbilgin, NO Claude trailer. Never delete assets — archive to `_archive~`.

## ⏯️ RESUME POINT (post-/clear: read this first)
- **SEQUENCING DECISION (2026-06-02, user-confirmed):** user chose "safe items first" over diving into the big graph/preview layer. NEW ORDER: **W7 HUD(+NRE) → W8 UI backdrops → W5/W6 combat juice+finisher → mob HitFlash**, THEN graph/preview deferred to a user-present focused session (it's the biggest/riskiest item, touches the verified core loop, has art forks + a real scene-based-MapFlowManager-vs-node-based-DungeonGraph reconciliation fork). Graph/preview spec still valid = `STAGING/PORTAL_PREVIEW_SYSTEM_SPEC_S6.md`.
- **NOTHING IN-FLIGHT** as of 2026-06-02 (clean checkpoint). All 3 scenes (_IsoGame/Map02/Map03) saved with full env art; console 0.
- **DONE so far:** cliff STAGE-A (clean lip), tooling fixes, asset-integrity (grounded), game-loop spine (RUNTIME-verified), multi-map (RUNTIME-verified), env art (portal/chasm/passage/stonecolumn all 3 scenes + reward-drop), **W7 HUD core (2 NRE FIXED+verified, HP/Rage/minimap/skill all bound+rendering)**. Only MOBS remain placeholder (intended).
- **NEXT CHAIN STEP:** State-audit workflow (wf_4bb08d37-81b) + cx/ax review produced a LOCKED P0→P1→P2 plan = **`STAGING/GAME_STATE_AND_PLAN_2026-06-02.md` (read this for the live ordered queue)**. **P0 DONE+VERIFIED 2026-06-02** (run-loop soft-lock/timeScale fixes, RuntimeRoomManager component removed from 3 scenes→MapFlowManager path, JuiceManager+SlashArcVFX wired 3 scenes, 13 mobs Knock+HitFlash, CharacterSelect gate, elite teleport bounds, reward fallback; runtime-verified console-0 + TRY-AGAIN soft-lock fixed). **NOW: P1** (order in plan doc §LOCKED: IsoSorter-flicker → audio-muted notice → HUD import → mob death-visual → skill cooldowns → RiftBreak AoE → death-split → map variety → Sundered Beat finisher (b) LAST). W8 backdrops: Victory verified, MainMenu wired (GPU crash blocked play-verify), Death=authored-panel (dimmed-game, fine).
- **WIRE RECIPE (placeholder→real art, reuse):** DestroyImmediate PlaceholderSprite comp; SpriteRenderer.enabled=true; sr.sprite=<spr>; sr.color=white; sortingLayer="Entities" (standing) or "Decals" (flat); order 0.
- **Resume verb:** read CURRENT_STATUS.md top block + this file; the auto-chain needs one user "devam et" after /clear (background-completion watchers don't survive /clear).

## Status legend: [ ] pending  [~] running  [x] done

### ✅ DONE & VERIFIED (2026-06-02)
- [x] **W1 Cliff** — STAGE-A clean floor-lip (spriteScale.y0.55 + shader alpha-melt + fade0.78/0.28 + offset-0.11 + fog) ; pillar-curtain gone. + black→slate kit repoint, flush, 14 orphan sets→_archive~, baker trim+rebake. STAGE-B (PixelLab proper iso-cliff regen) = OPTIONAL polish, queued.
- [x] **W2 Asset integrity** — 10 classes ×8-dir idle complete; 80 sprites measured-feet-pivot (SpritePivotBatchFix rewrite) → char GROUNDED; orphans archived. char actual=64px (canvas large = animation headroom).
- [x] **W3 Game-loop spine** — RUNTIME-VERIFIED: MainMenu→CharSelect→_IsoGame→Death/Victory; static PlayerClassManager.SelectedClass class-carry (Ranger carried, not forced-Warblade); Death MainMenu btn; _IsoGame build idx2.
- [x] **Multi-map foundation (= W4 V1)** — RUNTIME-VERIFIED: MapListSO + MapFlowManager (DontDestroyOnLoad, anti-repeat) + Map02/03 + RunStats blocker-fix; door→random map, RoomReached 1→2→3 accumulates, floor intact, 3-map→victory, clear→gate-opens.
- [x] **Env art (all 3 scenes)** — portal/chasm/passage/stonecolumn wired (_IsoGame+Map02+Map03) + reward-drop system (RewardPickup.cs+RunStats.RewardsCollected). Only MOBS placeholder (intended).
- [x] **Tooling** — Game View odd-res→1280×720 (GameViewSetup self-heal) + duplicate-global-light fix.

### ⏭️ REMAINING WORKFLOWS (ordered, auto-chain — one finishes → start next)
1. [ ] **Graph/Preview layer** (BIG, next) — StS2 per-run random DAG; **void preview-islands = REAL-MAP baked thumbnail** (dark tint + cyan-glow, shown deep between cliff & bg, parallax); next-only visibility (fog beyond) + **branch-count**; cyan-**orb travel**; **entry/exit gate design** (entry=spent/dim behind, exit=branch-count bright/locked-until-clear). Foundation is graph-friendly. Spec=`STAGING/PORTAL_PREVIEW_SYSTEM_SPEC_S6.md`.
2. [ ] **Map hazards** — blocking: Void Rift (=Chasm) ; walk-on DoT: void-corruption pool (#3A1A4A) + unstable-seal field (#00FFCC, flicker timing). Mechanics (cx) + PixelLab decals. Place in maps.
3. [x] **W7 HUD** — DONE+VERIFIED 2026-06-02: **2 NRE FIXED** (root cause = `PlayerResourceBase.OnResourceChanged` open-generic UnityEvent is NOT Unity-serialized → null at runtime → NRE on Add/RemoveListener; fix = inline `= new UnityEvent<int,int>()`, covers all 5 resource subclasses). Play-mode verified: 0 NullReferenceExceptions, resource bar now binds+updates. HP(Health)/Rage(resource)/minimap(self-binds DungeonGraph.Instance)/skill row all bound+rendering. REMAINING (folded into W8): on-brand bar/skill PACK art import (currently flat-color fallback — bars are red/orange flat; pack sprites `UI/RIMA/Pack/bar_frame_9slice`+`bar_fill` not imported).
4. [~] **W8 UI backdrops** (IN PROGRESS) — 4 backdrops EXISTED in `Assets/UI/Backgrounds/` (1024² Sprites) → moved to `Assets/Resources/UI/Backgrounds/` (Resources-loadable). Added shared helper `RimaUITheme.CreateFullScreenBackdrop` (AspectRatioFitter EnvelopeParent = cover/crop, no distortion, flat-tint fallback). WIRED: **Victory** (DemoCompleteOverlay) **= VERIFIED rendering** (`W8_victory_backdrop_v1.png`, throne-room art full-bleed + content on top). **MainMenu SCENE** (`Assets/Scenes/UI/MainMenu.unity` → `MainMenuController`, NOT the legacy `MainMenuScreen.cs`): added Backdrop to Root, **disabled ugly flat-cyan-square `RiftCrack` placeholder**, **fixed collapsed buttons** (VLG childControlHeight=true but buttons had height 0 → added LayoutElement preferredHeight=30, fits the 106px container). Scene saved+verified structurally (Backdrop sprite=main_menu_bg+EnvelopeParent, buttons LE=30, RiftCrack off). **Death** (DeathScreenManager) wired same mechanism. NOTE `MainMenuScreen.cs` (legacy DontDestroyOnLoad fallback) also got the backdrop call (harmless). HUD pack art (`bar_frame_9slice`/`bar_fill`) CONFIRMED loading (W7 note was wrong — on-brand bars already apply). **PENDING:** play-mode visual confirm of MainMenu + Death (blocked by a **D3D12 GPU-driver crash** on play-mode entry — see note; NOT caused by edits, stack = `D3D12CommandList::PrepareExecute`/`CheckDeviceStatus`). Resume by reopening Unity → play MainMenu → screenshot.
5. [ ] **W5 Combat completeness** — Warblade CombatHandler/finisher; mob HitFlash/Knockback/death (mob VISUALS stay placeholder per user).
6. [ ] **W6 Juice** — HitPause/ScreenShake/CameraPunch/DamageNumber.
7. [ ] **W9 Playtest + bugfix** — Gemini-MCP playtest → P0 list → fix → re-test.
8. [ ] **W10 Commit** — logical grouped commits at milestones (currently GATED).
9. [ ] **(optional) Cliff STAGE-B** — PixelLab purpose-built short iso-cliff strip (quality ceiling; needs credits/user-OK).

## Deferred (after playable)
- Animations (8-dir walk/attack/etc. — user: after playable). NOTE: char actual size=64px; canvas (120/124/128) intentionally large for animation headroom — see memory [[project-character-64px-canvas-large-for-animation]].
- Audio (user: "boşver" / defer)

## Queued — design investigations (post-playable, decide via workflow)
- **2D Townscaper-style sensible map/room auto-connection** (user 2026-06-02, ref X post https://x.com/Just_Game_Dev/status/2061012608665243750 — could NOT load, 402/auth; user to share content or retry). Want a 2D version: what's needed to connect rooms "sensibly" Townscaper-style. Techniques to evaluate: Wave Function Collapse (tile compatibility), irregular quad-grid, door-tile + pathfinding connection, marching-squares fit. Connects to existing [[project-roomtool-townscaper-s6]] pivot. Design via workflow when reached; likely feeds W4 (room/run loop) or a dedicated map-gen pass.
- **Trellis harness** — EVALUATED 2026-06-02, DECISION = SKIP (RIMA already has an equivalent project-brain: CLAUDE.md + PROJECT_RULES + CURRENT_STATUS + MEMORY auto-index + STAGING backlog + NLM canon + cx/ax dispatch loop). Adopting Trellis = high switching cost, overlapping function, marginal gain. Revisit only if the bespoke system breaks down.

## Current verified state (2026-06-02, end of art pass)
- 3 playable scenes (_IsoGame=4 mobs / Map02=5 / Map03=3), each: granite floor451 + clean dark-slate cliff lip + fog + Void_BG; full env art (portal/chasm/passage/column) + reward-drop. Mobs = placeholder squares (intended).
- Loop RUNTIME-verified: MainMenu→CharSelect(class-carry)→_IsoGame→clear→gate→door→random next map→…×3→victory ; die→Death→MainMenu/Retry. RunStats accumulates across maps.
- 10 classes grounded (feet-pivot). 4/10 have animator controllers (the demo-unlocked 4); other 6 = future controllers.
- KNOWN: HUD = placeholder boxes + 2 NRE (HUDController Start:98/OnDestroy:115) → W7. UI menu/death/victory = functional but no backdrop art → W8. Combat juice/finisher → W5/W6. commit GATED.
