# T8 Checker Floor + Generated Props Review

Verdict: FAIL

1. FAIL - `_Arena.unity` changed more than the requested `floorTileAlt` assignment. The expected checker assignment is present at `Assets/Scenes/_Arena.unity:479`, but the same diff also adds `overlayTiles: []` at `Assets/Scenes/_Arena.unity:481` and unrelated serialized `RoomRunDirector` fields/events at `Assets/Scenes/_Arena.unity:3487` and `Assets/Scenes/_Arena.unity:3500`. The task explicitly said the scene diff should be only `floorTileAlt`; this is a red flag.

2. PASS - `IsoRoomBuilder` checker behavior is null-safe and scoped. `Assets/Scripts/MapDesigner/Room/Runtime/IsoRoomBuilder.cs:285` computes `hasChecker = floorTileAlt != null`, and `Assets/Scripts/MapDesigner/Room/Runtime/IsoRoomBuilder.cs:288` falls back to `floorTile` when it is not assigned. The change only affects `groundTilemap.SetTile` in `BuildFloor`; overlay and cliff code still consumes `floorCells` later, so no overlay/cliff path side effect found.

3. FAIL - Editor utility does not register Undo. `Assets/Scripts/Editor/Map/PopulateGeneratedPropsMenu.cs:84` clears `room.props`, `Assets/Scripts/Editor/Map/PopulateGeneratedPropsMenu.cs:122` calls `EditorUtility.SetDirty(room)`, and `Assets/Scripts/Editor/Map/PopulateGeneratedPropsMenu.cs:134` calls `AssetDatabase.SaveAssets()`, but there is no `Undo.RecordObject(room, ...)` before mutation. Re-run idempotency is OK because `room.props.Clear()` runs before adding new placements.

4. PASS-WITH-NOTES - Prop placement avoids door-safety zones but does not keep clean center empty. The role generator marks center as `CleanCenter` at `Assets/Scripts/MapDesigner/Composition/CompositionRoleMapGenerator.cs:21` and assigns that role over the full bounds at `Assets/Scripts/MapDesigner/Composition/CompositionRoleMapGenerator.cs:29-36`; the auto-placer still gives `CleanCenter` a nonzero multiplier at `Assets/Scripts/MapDesigner/Props/Auto/BridsonPoissonAutoPlacer.cs:115-123`, so center props are expected. Samples: `elite_large_trident_01` has 90 props total, with clean-center placements like `Assets/Data/Rooms/Generated/elite_large_trident_01.asset:91` and `Assets/Data/Rooms/Generated/elite_large_trident_01.asset:103`; `combat_large_hourglass_01` has 65 props with clean-center placements at `Assets/Data/Rooms/Generated/combat_large_hourglass_01.asset:81`, `:105`, `:129`, `:135`; `combat_large_lshape_01` has 61 props with clean-center placements at `Assets/Data/Rooms/Generated/combat_large_lshape_01.asset:117`, `:165`, `:213`, `:219`, `:243`. No door-safety placements were found in the 15 generated rooms by a strict doorSockets parser. Recommendation: lower `TargetDensity` from `0.55f` to about `0.30f` and add a per-room cap around 45 props for large/elite rooms, or set `CleanCenter` density to `0f` if the center must stay visually clean.

5. PASS-WITH-NOTES - Generated `PropPlacementData` shape matches runtime expectations. The data class expects `propDefinitionGuid`, `tilePosition`, `rotationSteps`, `flipX`, `placedByUser`, and `variantIndex` at `Assets/Scripts/MapDesigner/Props/PropPlacementData.cs:9-14`; the menu writes those fields at `Assets/Scripts/Editor/Map/PopulateGeneratedPropsMenu.cs:112-118`; runtime resolves the GUID at `Assets/Scripts/MapDesigner/Room/Runtime/IsoRoomBuilder.cs:653`, positions from `tilePosition` at `Assets/Scripts/MapDesigner/Room/Runtime/IsoRoomBuilder.cs:668-669`, and applies `flipX` at `Assets/Scripts/MapDesigner/Room/Runtime/IsoRoomBuilder.cs:671-673`. Note: `variantIndex` is written by the menu but ignored by `IsoRoomBuilder`, which repicks variants using `StableTileSeed` at `Assets/Scripts/MapDesigner/Room/Runtime/IsoRoomBuilder.cs:659`.

Prop count summary checked via shell: 4-90 props per room; highest counts are `elite_large_trident_01.asset: 90`, `combat_large_hourglass_01.asset: 65`, `combatlarge_twin_basins_01.asset: 63`, and `combat_large_lshape_01.asset: 61`.
