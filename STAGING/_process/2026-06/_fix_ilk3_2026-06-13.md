# Demo İLK 3 — HIGH bulgu düzeltmeleri (2026-06-13)

Bağımsız review'da çıkan 2 HIGH bulgu cerrahi olarak düzeltildi. Demo bugün; minimal/balans-nötr yaklaşım.

## FIX 1 — Packet'siz projectile dalı balans-nötr

**Ne değişti:** `Assets/Scripts/Skills/PlayerProjectile.cs` ~satır 94-100.
Packet'siz dal artık `SkillRuntime.DealDamage(..., applyStatusMultiplier: false)` ile çağrılıyor (named argument; `isCrit` default `false` kalır).

**Neden minimal:** Mekanizma zaten vardı — `SkillRuntime.cs` ~107-130'daki overload `applyStatusMultiplier` parametresini (~144-149) destekliyor. Tek satırlık çağrı değişikliğiyle eski çıplak `hp.TakeDamage(damage)` davranışı geri geldi: incoming `StatusEffectSystem.damageMultiplierIncoming` çarpanı bu yolda UYGULANMAZ. SkillRuntime.cs'e DOKUNULMADI — diğer tüm `DealDamage` çağrıları default `applyStatusMultiplier: true` ile davranışını AYNEN korur. Tek-publish (OnDamageApplied + PublishSkillHit birer kez) DealDamage içinde değişmeden sürer.

**Test (data-proof, execute_code):**
`statusMult=2.0 | noMult(FIX)=100 (expect 100) | withMult=200 (expect 200) | PASS=True`
Packet'siz dal 2.0× amplifier altında bile ham 100 verdi (çarpan uygulanmadı); kontrast yol 200 verdi. Davranış değişikliği yalnızca bu dala izole.

## FIX 2 — DirectorMode DPS freeze (pause'da pencere donsun)

**Ne değişti:** `Assets/Scripts/UI/DirectorMode.cs`
- Yeni alanlar (~111-113): `telemetryPausedTotal`, `telemetryPauseStartTime`.
- `SetState` (~238): pause clock güncellemesi (`UpdateTelemetryPauseClock`) state değişiminden ÖNCE çağrılır.
- Yeni yardımcılar (CalculateTelemetryDps öncesi): `UpdateTelemetryPauseClock` (Director'a girince pauseStart kaydeder, çıkınca süreyi `telemetryPausedTotal`'a ekler) + `TelemetryClock()` = `Time.unscaledTime - telemetryPausedTotal`.
- Kayıt zaman damgası (`OnDamageAppliedTelemetry` ~eventTime): ham `telemetryEvent.UnscaledTime` yerine `TelemetryClock()` (damage yalnızca oyun akarken üretilir → canlı saat).
- `UpdateTelemetryDisplay` `now`, `OnTelemetryTargetDeath` TTK, `TelemetryDpsForValidation` → hepsi `TelemetryClock()` kullanır.
- `ClearTelemetry`: pause clock sıfırlanır; hâlâ pause'daysa pauseStart yeni baz ana çekilir (offset birikmesin).

**Neden minimal:** Sadece DirectorMode.cs içinde kalındı. Kayıtlar ve `now` artık aynı pause-arındırılmış eksende; 5sn'lik DPS penceresi pause süresince ilerlemez, dönüşte kaldığı yerden devam eder. SkillRuntime telemetri olayı şeması değişmedi.

## Doğrulama
- Unity recompile: tamamlandı, **0 compile error** (read_console Error filtresi = 0).
- EditMode testleri (assembly `RIMA.Tests.EditMode`, BİR KEZ): 541 test, **11 fail** (≤12 baseline, YENİ fail yok). Fail'ler tümü pre-existing ve değişen dosyalarla ALAKASIZ: V15g/V15h map designer (eksik PNG/dir), MCPSceneLoadModalBypass (private method imzası), PerformanceAntiPattern, PrefabHealth (interactRadius), SubRoomSequence (DontDestroyOnLoad edit-mode). Projectile/SkillRuntime/DirectorMode telemetri testi fail YOK.
- Console: 0 error. (Test run sonrası birkaç bilgilendirme log'u var — RoomLayoutData/RoomRunDirector — error değil, değişikliklerle ilgisiz.)
