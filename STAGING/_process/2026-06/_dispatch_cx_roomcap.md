ACTIVE RULES: think→min→surgical→BLOCKED if unclear. READ-ONLY: koda/git/sahneye DOKUNMA, Unity'yi SÜRME (statik kod + graphify only), `git add/commit` ASLA.
NLM ACCESS: NB=$(cat .claude/nlm.local); uvx --from notebooklm-mcp-cli nlm notebook query $NB "<soru>"
GRAPHIFY query-first (~71× ucuz): graph.json = `STAGING/_process/2026-06/graphify_fullmap/graphify-out/graph.json`.

# Amaç — CX LENS: feasibility / reuse / kesin entry-point
Tam brief'i OKU: `STAGING/_process/2026-06/council_room_cliffisland_capture_2026-06-18.md` (4 soru orada).
Senin merceğin: **uygulanabilirlik + kod-yeniden-kullanım + kesin programatik entry-point.**
- Bir oda JSON'unu "cliff'li yüzen ada" yapan KOD yolunu dosya:satır ile çıkar (IsoRoomBuilder/RuntimeRoomManager/RoomLoader/LiveRoomReloader — graphify ile çağrı zinciri).
- Cliff auto-generate fonksiyonunu bul; execute_code ile TETİKLENEBİLİR public/erişilebilir API var mı? (imza + dosya:satır)
- _Arena'yı KİRLETMEDEN 6 odayı yakalamanın EN AZ-RİSKLİ kod adımları (mevcut metotları reuse ederek). Hangisi gerçekten çalışır, hangisi tuzak?
ÇIKTI dosyası: `STAGING/_process/2026-06/_council_cx_roomcap.md`. Dönüş ≤10 satır.
