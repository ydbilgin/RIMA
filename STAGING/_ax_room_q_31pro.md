You are a senior roguelite game architect advising on RIMA, a 2D iso-styled top-down roguelite ARPG (Unity URP 2D, PPU64, custom-axis depth sort, iso floor Tilemap cellSize 0.96 x 0.585). Answer as a concrete design recommendation, not generic theory. Be opinionated and pick winners.

CURRENT STATE (verified):
- Map flow: a singleton MapFlowManager picks a RANDOM SCENE from a list of 6 baked iso "floating island" Unity scenes. 3 maps per run, victory on 3rd clear. Anti-immediate-repeat. Each map is a separate .unity scene with a painted iso floor tilemap + auto-placed cliff sprites (directional SW/SE + inward tuck) + EdgeCollider2D boundary.
- The 6 shapes (bridge/cross/ell/hourglass/donut/diamond) were generated by a ONE-OFF procedural script: a floor predicate (which cells are floor) -> auto-cliff -> auto-boundary -> save scene. The generic shape-agnostic pieces (floor paint, auto-cliff fit, auto-boundary trace) WORK and are proven. The generator was NOT saved as a reusable tool.
- Disconnected/half-built infra exists: RoomData (ScriptableObject, per-cell floor/cliff/wall/prop/portal) + RoomDataJson (JSON DTO read/write, but ZERO room json files on disk), RoomType enum (Combat/Elite/Boss/Chest/Merchant/Forge/Event/Curse/Corridor), RoomTypeData SO (portal weights), an editor-only walled top-down RoomBuilder, and a prefab-based RoomLoader/RoomRegistry that is NOT used by the live scene flow.

DECISION ALREADY MADE: move to a DATA-DRIVEN room system (one generic Arena scene + data definitions; "drawing a room" = authoring data, not making a scene).

USER REQUIREMENTS: lots of rooms; demo has randomness BUT logical rooms (NOT all square — transitional/flowing organic shapes); include big rooms, a boss room, an elite room, a reward room; must be cheap to multiply rooms later.

ANSWER THESE, concretely:
1. DEFINITION FORMAT: ScriptableObject vs JSON vs hybrid for a room definition? Weigh hand-authoring, git-diff, runtime load, designer-tool export, and ease of mass-producing rooms. Pick one and justify.
2. SHAPE REPRESENTATION: parametric predicate (compact, procedural variety) vs per-cell mask (full control / hand-drawable) vs hybrid? Which best yields LOGICAL non-square FLOWING rooms cheaply AT SCALE? Recommend a concrete scheme (e.g. a small set of shape archetypes with params, or an ASCII/bitmap mask grid, or noise+constraints).
3. RUN STRUCTURE: how to get "random but logical"? Recommend the structure: a curated TYPE sequence (e.g. start -> combat -> combat -> elite -> reward -> combat -> boss) with each slot filled by a random layout from that type's pool? Linear vs branching (StS DAG / Hades door-choice)? What is the RIGHT scope for a DEMO specifically (keep it lean)?
4. ROOM TYPES + SIZE: which types for the demo, and how to encode TYPE + SIZE (small/medium/large/big-arena) into the definition? How many distinct layouts PER TYPE for a satisfying demo (give numbers)? What gameplay/visual traits distinguish boss vs elite vs reward vs combat rooms (size, chokepoints, openness, props)?
5. MULTIPLICATION WORKFLOW: after the system is built, what is the EXACT minimal action to add ONE new room? Describe the ideal authoring loop.
6. RISKS / PITFALLS: name the top 3 risks of this approach and how to avoid them.

Keep it tight and decisive. Use short sections matching the 6 questions.
