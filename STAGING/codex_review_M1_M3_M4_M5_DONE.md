# Codex Review M1+M3+M4+M5 - DONE Report

## VERDICT
**Status:** PASS-WITH-CONDITIONS
**Confidence:** 78%
**Reason (1 sentence):** The architecture is coherent enough to proceed after targeted spec edits, but Sprint 9/10 RoomTemplate contracts, current-code file paths/type names, and the Wang dispatch contract have blocking inconsistencies that should be fixed before Opus implementation or M6 dispatch.

## A. INTERNAL COHERENCE
- M1: PASS-WITH-CONDITIONS - The lock is mostly self-consistent: Wang 16 is declared as the replacement topology, Sprint 10 is placed before Natural Engine, and the non-negotiables are repeated in the seal. Evidence: `sprite_strategy_FINAL_LOCK.md:63-94`, `sprite_strategy_FINAL_LOCK.md:403-431`, `sprite_strategy_FINAL_LOCK.md:608-615`. Minor issue: the tag phrase `wang_{NESW_bitstring}` at `sprite_strategy_FINAL_LOCK.md:94` is ambiguous because the actual numerology is corner order `NE-NW-SE-SW` at `sprite_strategy_FINAL_LOCK.md:66`.
- M3: PASS-WITH-CONDITIONS - Deliverables map to M1 data/importer/P0 scope, but the spec has implementability drift against the live codebase. It asks for a new `TargetLayer.cs`, while a `TargetLayer` enum already exists at `Assets/Scripts/MapDesigner/Brush/Data/Enums.cs:17`. It points R1 to `Brush/Execution/DecorativeExecutorUtility.cs`, but the live utility is nested in `Assets/Scripts/MapDesigner/Brush/Executors/Editor/FreeformDecalExecutor.cs:23` and applies scale at lines 144 and 157. It points R2 to `Assets/Scripts/MapDesigner/Brush/Validation/RimaSortingLayerValidator.cs`, while the live validator is `Assets/Editor/RimaSortingLayerValidator.cs:8-12`.
- M4: PASS-WITH-CONDITIONS - The RoomBank deterministic pick and save/load round-trip targets are executable in principle, and the PlayMode test explicitly excludes nav checks. However, M4 says Sprint 9 delivered only `schemaVersion`, `roomId`, `biomeId`, `roomType`, and `bounds` at `codex_brush_sprint10_room_template_bank.md:21`, while M3's Sprint 9 stub already includes sockets, camera bounds, prefab, and tags at `codex_brush_sprint9_atlas_importer.md:253-273`. That makes the Sprint 9 -> Sprint 10 diff unclear.
- M5: FAIL-CONDITION - The Wang numerology content matches M1, but the paste-ready PixelLab parameter table is not complete for the current tool contract because `transition_size=1.0` requires a transition/blend description. M5 lines `pixellab_l3_wang_chain.md:20-28` list only tile size, lower/upper descriptions, transition size, and base IDs. Also, M5 simultaneously says expected output is 16 base cases at `pixellab_l3_wang_chain.md:30-31` and asks QC to verify coverage `0000-1111 minus 0000` at `pixellab_l3_wang_chain.md:142`, which is 15 cases.

## B. CROSS-DOCUMENT CONSISTENCY
- M1 <-> M3: Mostly aligned for `BrushAssetVariant`, `AssetPoolSO`, importer order, validator severity, preview, and P0 retrofits. M3 preserves the M1 P0 fixes: native scale path at `codex_brush_sprint9_atlas_importer.md:282-287` and sorting layer extension at `codex_brush_sprint9_atlas_importer.md:293-296`, matching `sprite_strategy_FINAL_LOCK.md:378-397`. Condition: update M3 file paths and avoid duplicate enum creation.
- M1 <-> M4: The top-level RoomTemplate fields match M1 Section 4 (`schemaVersion`, `roomId`, `biomeId`, `roomType`, `bounds`, sockets, camera, prefab, tags) at `sprite_strategy_FINAL_LOCK.md:218-232` and M4 at `codex_brush_sprint10_room_template_bank.md:45-59`. Condition: M4's helper-type details should be backported into M1 or M3 to avoid Sprint 10 type churn.
- M3 <-> M4: Fails as written. M3 stub helper types use `DoorSocket.position`, `Vector2 direction`, `int size`; M4 changes that to `socketId`, `DoorDirection direction`, and `widthInTiles` at `codex_brush_sprint10_room_template_bank.md:67-77`. M3 uses `PlayerSpawnSocket.facing` as `Vector2`; M4 uses `DoorDirection` at `codex_brush_sprint10_room_template_bank.md:83-86`. M3 uses `CameraBounds.bounds`; M4 uses `CameraBounds.tileRect` at `codex_brush_sprint10_room_template_bank.md:99-104`. This is a breaking rename/type-change path unless Sprint 9 is reduced to a true thin stub or updated to M4's final helper types.
- M1 <-> M5: Wang case meanings match exactly across M1 (`sprite_strategy_FINAL_LOCK.md:66-82`) and M5 (`pixellab_l3_wang_chain.md:62-80`). Condition: standardize the tag label and the inclusion/exclusion of `0000`.
- M3 <-> M5: The pipeline intention matches: M5 outputs `master.png`, Sprint 9 importer uses `L3_Wang16_Topdown.asset`, and the AssetPool receives `wang_{bitstring}` variants at `pixellab_l3_wang_chain.md:216-228`. The weak point is grid contract: M3 says WangSliceGenerator may auto-detect grid size at `codex_brush_sprint9_atlas_importer.md:176`, while M5 expects 16-23 tiles but does not define grid layout, metadata schema, or how 7 transition extras are ignored.

## C. MISSING / GAPS
- M3 does not account for the existing `TargetLayer` enum in `Assets/Scripts/MapDesigner/Brush/Data/Enums.cs:17`. A new enum file with the same type name will compile-fail unless the existing enum is modified instead.
- M3 does not account for the existing `AssetPoolSO` fields beyond `sprites`: current live fields are `sprites`, `spriteWeights`, `tiles`, `prefabs`, `nativeSize`, `supportsRotation`, `supportsFlip`, and `isSoftEdge` at `Assets/Scripts/MapDesigner/Brush/Data/AssetPoolSO.cs:10-19`. The migration spec should say which fields remain, map, or deprecate.
- M3's P0 file paths are wrong against live code. `RimaSortingLayerValidator` is under `Assets/Editor/RimaSortingLayerValidator.cs`, not the M3 file-scope path. `DecorativeExecutorUtility` is nested inside `FreeformDecalExecutor.cs`, not a standalone `Execution` file.
- M4's `RoomType` surface is unresolved against live `RIMA.RoomType`, which contains `Combat`, `Elite`, `Boss`, `Chest`, `Merchant`, `Forge`, `Event`, `Curse`, and `Corridor` at `Assets/Scripts/Core/RoomType.cs:7-18`. M3 proposes a separate enum `{ Combat, Shop, Shrine, Elite, Boss }` at `codex_brush_sprint9_atlas_importer.md:268`. The spec needs either a namespace-isolated enum or a mapping to global `RIMA.RoomType`.
- M5 is missing the transition/blend description parameter required when using `transition_size > 0`. It should add a concrete `transition_description`, likely similar to `moss-fringed stone wall transition with readable floor-to-wall edge`.
- M5 does not define how to label or discard the 7 extra transition tiles when the importer requires 16 deterministic Wang variants.
- M1 Section 15 unresolved items are not Sprint 9/10 blockers except actual PixelLab credit cost for dispatch; the L4 master count and additional biome count are explicitly post-vertical-slice decisions.

## D. EXECUTABILITY
- M3 (Sprint 9) realistic? Conditional. The feature scope is large but feasible in 1.5-2 days only if the file-path/type issues are fixed first and the preview window remains skeletal. Without those corrections, implementation will spend time resolving compile errors and stale paths.
- M4 (Sprint 10) realistic? Conditional. The 1-1.5 day estimate is realistic if Sprint 9 either delivers the final helper types or a genuinely thin stub. It is not realistic if Sprint 10 must migrate field names/types immediately after Sprint 9 writes a broader incompatible stub.
- Minimum vertical slice loop subset: M3 must include `BrushAssetVariant`, `AssetPoolSO` variants with legacy fallback, `BrushRadiusProfileSO`, `L3_Wang16_Topdown`, `BrushAtlasImporter`, `BrushAtlasValidator`, no-runtime-scale path, sorting-layer validator update, and RoomTemplate minimal/final fields. M4 must include `RoomTemplateSO`, `RoomBankSO.Pick`, save/load, validator, and one PlayMode runtime test. Preview polish, right-click PNG integration, palette/outline drift heuristics, and all non-L3 templates can be cut if schedule slips.
- Open question priority list (Opus signoff): (1) RoomTemplate Sprint 9 stub vs Sprint 10 final helper types, (2) reuse/global mapping for `RoomType`, (3) exact Wang bit order and whether `0000` is imported, (4) PixelLab transition_description and grid/output metadata contract, (5) actual file path ownership for sorting validator and decorative utility.

## E. RISKS + DRIFT
- Meta Risk sorting layer mismatch is addressed in M1/M3/M4, but M3's file path is stale. Live validator currently creates only Patch and Scatter at `Assets/Editor/RimaSortingLayerValidator.cs:10-11` and `Assets/Editor/RimaSortingLayerValidator.cs:36-48`, while live decorative code emits Detail/Accent/Patch at `Assets/Scripts/MapDesigner/Brush/Executors/Editor/FreeformDecalExecutor.cs:237-247`.
- Meta Risk runtime scale is addressed in M1/M3. Live code confirms it is real: `BrushLayerOperation.scaleRange` defaults to `0.85f, 1.15f` at `Assets/Scripts/MapDesigner/Brush/Data/BrushLayerOperation.cs:15`, and `DecorativeExecutorUtility.PlaceAt()` lerps and applies it at `Assets/Scripts/MapDesigner/Brush/Executors/Editor/FreeformDecalExecutor.cs:144-157`.
- Meta Risk GUID stability is addressed in M3/M4 through importer reimport and RoomTemplate save/load tests, but the spec should explicitly preserve the existing `AssetPoolSO` asset and its current fields.
- Meta Risk test pollution is addressed: M3 uses `Assets/TempTests/Brush/` and M4 uses `Assets/TempTests/Room/...`.
- Meta Risk RoomTemplate legacy placeholder is addressed in M4; live legacy template is indeed only layout/width/height at `Assets/Scripts/Systems/Map/RoomTemplate.cs:7-12`.
- Meta Risk L3 wall semantics moved from under-specified horizontal-only to Wang 16 in M1/M5. The remaining drift is only bitstring/tag standard and grid layout.
- Workflow override is reflected correctly: docs say Opus implements and Codex reviews in the 16-18 May window, e.g. M1 `sprite_strategy_FINAL_LOCK.md:37` and M4 `codex_brush_sprint10_room_template_bank.md:14`.

## F. BLOCKING CONCERNS (Opus implement oncesi cozulmeli)
1. Location: `codex_brush_sprint9_atlas_importer.md:253-273` and `codex_brush_sprint10_room_template_bank.md:21`, `67-104`. Question: Is Sprint 9 RoomTemplate a thin 5-field stub or the full socket/camera/tag data model? Recommendation: Prefer defining the final helper types in Sprint 9 and making Sprint 10 add behavior only. If that is too much, reduce Sprint 9 to exactly `schemaVersion`, `roomId`, `biomeId`, `roomType`, and `bounds`.
2. Location: `codex_brush_sprint9_atlas_importer.md:268`, `Assets/Scripts/Core/RoomType.cs:7-18`. Question: Should RoomTemplate use global `RIMA.RoomType` or a new room-library enum? Recommendation: Use global `RIMA.RoomType` for V1 and map Shop/Shrine to existing Merchant/Event or add those values deliberately in one place.
3. Location: `codex_brush_sprint9_atlas_importer.md:331-353`, `Assets/Scripts/MapDesigner/Brush/Data/Enums.cs:17`, `Assets/Editor/RimaSortingLayerValidator.cs:8`, `Assets/Scripts/MapDesigner/Brush/Executors/Editor/FreeformDecalExecutor.cs:23`. Question: Are the implementation paths current? Recommendation: Patch M3 to modify existing `Enums.cs`, `Assets/Editor/RimaSortingLayerValidator.cs`, and `FreeformDecalExecutor.cs`, or explicitly create wrapper files and update call sites.
4. Location: `pixellab_l3_wang_chain.md:20-31`. Question: What exact MCP payload should be dispatched? Recommendation: Add required `transition_description`, plus chosen `view`, `detail`, `shading`, and `outline` defaults if the dispatch should be paste-ready.
5. Location: `sprite_strategy_FINAL_LOCK.md:66`, `sprite_strategy_FINAL_LOCK.md:94`, `pixellab_l3_wang_chain.md:83`, `pixellab_l3_wang_chain.md:142`. Question: Is the canonical bit order `NE-NW-SE-SW`, and is `0000` imported? Recommendation: Rename the tag rule to `wang_{bits_ne_nw_se_sw}` and decide either 16 imported cases including `0000`, or 15 imported wall cases with all docs updated away from '16 base'.
6. Location: `codex_brush_sprint10_room_template_bank.md:111-132` and `codex_brush_sprint10_room_template_bank.md:314`. Question: Is `RoomBankSO` editor-only, runtime-readable, or both? Recommendation: Say 'editor-authored runtime-readable asset' and keep editor utilities (`Saver`, `Loader`) in Editor-only assemblies.

## G. RECOMMENDED ACTIONS
1. M1 / M3 / M4 / M5 edits: Patch M3 for current file paths, existing enum reuse, AssetPoolSO migration details, and RoomTemplate helper type alignment. Patch M4 to remove the false 'Sprint 9 thin stub' assumption or make M3 actually thin. Patch M5 with required MCP payload fields, grid/output metadata, and 16-vs-15 case decision. Patch M1 only for the tag wording if `NESW_bitstring` remains ambiguous.
2. Open questions for Opus signoff: RoomTemplate helper type finalization, RoomType mapping, Wang bit order/0000 import, PixelLab transition_description/grid output, and RoomBank runtime/editor wording.
3. Sprint 9 implementation green-light ediliyor mu? Not yet. Green-light after blockers 1-3 are patched.
4. Wang tileset dispatch (M6) green-light ediliyor mu? Not yet. Green-light after M5 includes a complete MCP payload and resolves 16-vs-15 Wang coverage.
