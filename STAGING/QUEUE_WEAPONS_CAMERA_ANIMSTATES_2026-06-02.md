# RIMA — User-Given Ordered Queue (2026-06-02, live)

> Rules: do IN ORDER, one at a time. Reviews via **cx (laurethayday)** + **ax (Gemini 3.5 Flash, rotate the 5 profiles)** — before and/or after the workflow. **PixelLab: NO production now — only tespit (analysis) + documentation.** Animation/weapon GENERATION = later together-with-user session. Keep a decision document.

## ✅ DONE
- **Q1 — Character WEAPONS (determination):** NLM-canonical table (10 classes) recorded below in this file's history + plan. Warblade greatsword + Ronin katana already produced in PixelLab; other 8 = queued (produce later, together).
- **X-video (@stopsignalart) examined:** 35s SILENT pixel **game** clip = player DIGS terrain + cyan liquid fills dug pool with **3D-real fluid feel** + **Townscaper-style smart auto-connect digging**. Technique: cellular-automata/falling-sand fluid (per-cell water, gravity flow + lateral spread → **level-equalization** = the "3D real" trick) + 2-3 tone depth shading + traveling shimmer; smart-dig = grid auto-tiling (bitmask/marching-squares/Wang/dual-grid) + flood-fill pool merge. **= the 2D-Townscaper map reference user wanted** (ties to backlog Townscaper item). Files in `C:\Users\ydbil\rima_refs\`. OPEN Q: use as (a) env/VFX ref or (b) general technique bar?
- **Q2 — Camera FOLLOW fix:** root cause = Main Camera had `[Obsolete] RIMA.CameraFollow`, not live `RIMA.CameraSystem.CameraFollow`. Swapped to live (auto-acquires Player, juice offsets) on all 3 scenes. RUNTIME-VERIFIED: camera tracks player (camPos.xy == playerPos.xy).
- **Q2b — Camera FRAMING (user: zoom out + player below center):** PixelPerfectCamera refRes 320×180→**640×360** (canonical lock restored, was a regression) = zoom out; `CameraFollow.worldOffset`=(0,**+1.2**,-10) = player below center. 3 scenes. VERIFIED: player viewport y=0.29 (below center), zoom ortho 2.81. ⚠️ Screenshot revealed player SPAWNS at (-2.07,3.10) = SW cliff/void corner, not floor-center → see Q6.

## ✅ DONE (cont. 2026-06-02)
- **Q6 — Boundaries + spawn:** root cause = Player + RewardSpawnPoint AUTHORED at SW void corner (-2.07, 3.10); no spawn-manager (authored pos sticks). FIX (cx laurethayday, 3 scenes): Player + RewardSpawnPoint → Ground diamond center (1.92, 8.19). IsoFloorBoundary already matched diamond (centroid within 2u) → LEFT. VERIFIED: edit-mode player inside diamond; play-mode player runtime pos (1.92,8.19), camera follows (1.92,9.39 = +1.2 offset), 0 console errors.
- **Q5 — Clear-drop:** RESOLVED by Q6. Root cause = reward spawned at the void-corner RewardSpawnPoint (off-screen). Sprite/layer/collider were all fine. PLAY-VERIFIED: killed 3 mobs → remainingMobs 0, rewardSpawned True, `RewardPickup` at (1.92,8.19) center, sprite=reward_relic, Entities layer, active CircleCollider trigger, persists on ground (no auto-collect on overlap). NOTE for combat backlog: dead enemies lingered (still tagged Enemy after Health.OnDeath fired) — idle undefended player got swarmed→death screen (test artifact, known idle behavior).

## ✅ DONE (cont.)
- **Q4 — Cliffs re-fix:** DONE (after long detour). FINAL solution (user-directed): clean ALL old cliffs (procedural MESH approach + extended CliffVoidFade shader ABANDONED — mesh rendered dark/lit-in-void + fragile; old CliffTilemap+CliffAutoPlacer disabled) → place **`CliffKit_RefB_pixelified`** sprites (user-chosen pixel-art version, copied from `STAGING/_archive/s106_overnight/ref_kit_b_pixelified` → `Assets/Sprites/Environment/CliffKit_RefB_pixelified/`, imported Sprite/PPU64/Point/TopCenter-pivot) MATHEMATICALLY: for each floor cell, for each void-facing FRONT direction (S=(-1,-1)/SE=(0,-1)/SW=(-1,0)/E=(1,-1)/W=(-1,1)), place the matching `cliff_<DIR>` sprite at the FLOOR cell's `GetCellCenterWorld` (floor-side, NOT void-side), top-pivot hanging down (y-0.10), sorting Entities order=(16-y)*2. = continuous thick natural rock underside under the floor. **241 sprites/scene, all 3 maps** (_IsoGame/Map02/Map03), saved. Screenshot `Assets/Screenshots/Q4_cliffsprites_pixelified_v1.png`. KEY LESSONS: (a) CliffOverlays container left INACTIVE → sprites invisible (GameObject.Find skips inactive, FindObjectsByType skips inactive SR); (b) custom lit URP-2D shader renders BLACK in the void (no 2D light) → use UNLIT or just sprites; (c) PixelLab create_tiles_pro for cliff face = over-cyan + dark-bottom, not worth it; (d) user wanted SPRITES placed mathematically, not a mesh. ax-flash-fix + cx_dispatch-flash-fix both DONE+verified.

## ⏭️ PENDING — (Q4 was here, now DONE above)
- ~~Q4 cliffs~~ (see DONE)
  - **4-way DIAGNOSIS (me + ax 3.1 Pro + Gemini 3.5 Flash + cx laurethayday) — converged:** ROOT = (1) all 52 cliff cells render the SAME `cliff_S` sprite (DirectionalCliffTile.cs:99 first-branch-wins; placer paints floor cell not void cell) → corners get south art = slivers/non-continuous; (2) `transformOffset.y=-0.11` (DirectionalCliffTile_Hades.asset:31) = measured 0.110u gap floor-lip↔cliff; (3) filters (hasNorthVoid/voidNeighbors>=5/monotonic-south, CliffAutoPlacer.cs:277-385) drop 11 of 63 edge cells → floating chunks; (4) 128×192 sprite (2×3 cells) placed per-cell → overlap; (5) CliffVoidFade shader amplifies. STAGE-A y0.55 squash KILLED depth.
  - **FIX consensus:** continuous skirt, NOT per-cell tiling. 2 Geminis→baked-art-per-edge (Option ii); cx→procedural extrusion (Option iv). SYNTHESIS = baked-skirt ART placed along real floor-edge polyline flush to lip, bypass broken tilemap directional system.
  - **PIPELINE (user-endorsed):** imagegen depth-preview (cheap, explore look) → user picks → PixelLab final (chosen preview as INIT-ref + floor451 as STYLE-ref → guaranteed pixel-match, no quantize gamble). Respects PixelLab-gen-gated (imagegen now autonomous, PixelLab=together-session).
  - **PixelLab tools (rima-asset audit, `STAGING/Q4_CLIFF_ASSET_PACK_PROMPTS.md`):** `create_image_pro`=WEB-ONLY (not MCP). MCP path = `create_tiles_pro` (iso, style_images=floor451, batch: straight+variant+inner/outer corner) + `create_map_object` (corner caps + cyan seam; weak perspective→must say "rock face below floor", try view="side"). `create_isometric_tile` 64px max=excluded. Pack=5-6 pieces @192px, face ≥128px.
  - **NOW:** 3 imagegen depth-previews generating (VAR-A stratified-wall [preferred] / VAR-B stepped-shelves / VAR-C broken-masonry) → `STAGING/imagegen/Q4cliff_var{A,B,C}_*.png`. User picks → PixelLab final in together-session.
  - **PENDING user calls:** (a) which VAR, (b) interim cheap code-fix Y/N (cx fix direction+0.11 gap+filters on existing kit so not-broken meanwhile). 3 scenes for final placement.

## ⏭️ PENDING — BIG documentation tasks (workflow + cx/ax reviews + DECISION DOC, NO PixelLab production)
- **Q7 — WEAPON production plan (detailed, workflow):** for all 10 classes: WHERE produced (which PixelLab tool/method — create_image_pro init+style-anchor per NLM, or create_object, etc.), WHAT canvas/Unity size (per Q1 table), HOW placed/attached (decoupled WeaponSR child, 8-dir-by-code vs 1-dir runtime-rot, pivot, sort, swing-fade). Workflow → cx+ax review → re-think → decision doc. Check PixelLab MCP + API docs.
- **Q8 — ANIMATION STATES list (detailed, workflow):** Based on the 2 PixelLab UI screens user shared:
  - **Add Animation screen (Image #1):** preset types = MOVEMENT(Idle/Jumping/Running/Walking[19 opt]/Backflip/Crouching/FrontFlip/GettingUp/Slide) · COMBAT(Kicking[5]/Punching/Reactions[3]/Fireball) · INTERACTIONS(Drinking/PickingUp/Pull/Push/ThrowObject) · CUSTOM(Custom Animation V3 = action desc + frameCount 4-16 + "Keep first frame (idle pose)" + **Start Frame + End Frame** custom frames; Custom Pro). Cost ~2 gen/direction (V3, 8 frames). char = 120×120 8-dir mannequin.
  - **Create State screen (Image #2):** edit char with text desc applied across all 8 dirs ("Use color palette from reference"), cost 20-40 gen. States = pose/appearance variants.
  - **DELIVERABLE (list):** (a) which STATES to create per class (e.g. attack-windup pose, impact pose, hurt, death, cast); (b) which state → which animation; (c) which animations use **Start+End frame** (provided via states → AI interpolates wind-up→impact); (d) which use preset types vs Custom V3; (e) per-class animation set for the demo-4 first. + prepare the **Custom V3 action-description PROMPTS** (user runs the gen; I only write prompts) + the **state LIST** (user approves → I MCP-produce later, NOT now).
  - Check PixelLab MCP tools (`animate_character`, `create_character_state`, `create_character`, `agent_help`) + API docs (start/end frame support? costs? params?).
  - Workflow → cx+ax review (before &/or after) → decision doc.

## After Q7/Q8 (separate, NOT autonomous)
PixelLab GENERATION (weapons + states + animations) = together-with-user session ([[feedback-never-animate-without-approval]]). Finisher (Sundered Beat) rides along. Animation gen: I prepare prompts (Custom V3); states: I list, user approves, I MCP-produce.

## Deferred autonomous P1 (resume when above clears): IsoSorter flicker, audio-muted notice, HUD import verify, mob death-visual, skill cooldowns, RiftBreak AoE, death-split wire, map variety.
