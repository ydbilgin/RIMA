ACTIVE RULES: think before answering; concise; no code.
NLM ACCESS (if needed): uvx --from notebooklm-mcp-cli nlm notebook query 30ddffa5-292f-4248-8e77-68074af901be "<q>"

# Role
ROUND 2 — deeper INDUSTRY / ANIMATION-BREAKDOWN consult. Round-1 gave references (Hades chamber gates, Dead Cells teleport, Bastion floating paths, StS map). DO NOT repeat — go DEEPER on the HOW: what exactly gets animated, by which layer, and the production breakdown a SMALL team would build. Read by Opus, synthesized with a parallel technical (Codex) consult.

# LOCKED DIRECTION
- Doors -> PORTALS (color + floating rune-icon center; count = branch count).
- Travel = morph avatar into a glowing ORB (portal color), zip across void, crash-land, illuminate target island. < 0.8s, camera sweeps ahead.
- Upcoming rooms shown DIEGETICALLY as REAL iso room-islands floating DARK / STATIC / MOB-LESS in the void; illuminate + scale up only when chosen.
- Map-fragment repurposed as scout/reveal-further currency (basic next-step always free).

# QUESTIONS (per-Q: tight breakdown + the 1-2 best reference games + the pitfall)
Q1. TELEPORT/MORPH — break down HOW shipped games animate avatar -> travel -> arrive. For Dead Cells, Hades, Enter the Gungeon, Children of Morta (and Diablo if useful): WHAT exactly is animated and BY WHICH LAYER — character squash/dissolve sprite? a separate orb/projectile sprite? VFX particle trail? camera move? full-screen FX/flash? Then give a RECOMMENDED RIMA recipe: the MINIMUM set of animated elements for maximum juice (so a small team isn't animating everything).
Q2. DIEGETIC PREVIEWS — how do games present upcoming-location previews in-world (Bastion floating paths, Hades, Ori, Moss, Tunic)? On the dark preview islands, what (if anything) is animated — idle shimmer / nothing / glow-on-hover only? How to keep them readable and NOT distracting while combat happens on the main island.
Q3. ANIMATED vs STATIC LIST — give a clear prioritized list for the WHOLE flow: which elements MUST be truly animated (portal swirl, orb morph, comet trail, crash-land impact, hover-glow) vs which stay STATIC (dark preview islands, far parallax). Order by juice-per-effort for a small team.
Q4. PACING — recommended timings (ms) for each beat: room-clear reveal, portal hover-feedback, morph-in, void travel, arrival/illuminate. So a 10-min loop isn't slowed by repetition.

# OUTPUT
Per-Q as above, then a final "RECOMMENDED ANIMATION/PRESENTATION RECIPE" paragraph + the single biggest open question for the user. Concise.
