ACTIVE RULES: (1) think before coding (2) min code, no speculation (3) surgical (4) BLOCKED if unclear.
NLM ACCESS: uvx --from notebooklm-mcp-cli nlm notebook query 30ddffa5-292f-4248-8e77-68074af901be "<question>". Direct-read: code/scenes/STAGING/memory.

Amaç: Data-driven scene-per-map loop — kapıdan geçince RANDOM sonraki map yüklenir, map'ler düzenlenebilir bir MapList'te tutulur, run boyunca ilerleme birikir, _IsoGame authored kalır (teardown YOK). Unity açık, UnityMCP profilinden eriş. (3-ajan research bunu doğruladı; adımlar kesin.)

KRİTİK BLOCKER (atlanırsa loop çalışmaz): RunStats.OnSceneLoaded (Assets/Scripts/Core/RunStats.cs ~92-107) HER sahne yüklemesinde ResetRun çağırıyor → her map geçişinde roomReached/kills/time SIFIRLANIR. Bunu gate'le.

## ADIMLAR
1. **MapListSO** — YENİ `Assets/Scripts/Systems/Map/MapListSO.cs`: `[CreateAssetMenu(menuName="RIMA/Map/Map List")] public class MapListSO : ScriptableObject { public List<string> mapSceneNames=new(); public string startSceneName="_IsoGame"; }` (saf data).
2. **Duplicate map sahneleri** — `Assets/Scenes/_IsoGame.unity`'i AssetDatabase.CopyAsset ile `_IsoGame_Map02.unity` + `_IsoGame_Map03.unity` olarak kopyala. Her kopyada mob konumlarını/sayısını GÖRÜNÜR şekilde değiştir (transition'ın çalıştığı belli olsun — örn Map02'de mob'lar farklı yerlerde, Map03'te 1 fazla/eksik). Floor/IsoGrid/RoomClearVictoryTrigger KORU. _IsoGame'e DOKUNMA. RoomLoader EKLEME.
3. **Build Settings** — _IsoGame_Map02 + _IsoGame_Map03'ü ekle (enabled). EditorBuildSettings.scenes API tercih.
4. **MapList asset** — `Assets/Data/Map/MapList_Act1.asset` yarat, mapSceneNames=["_IsoGame","_IsoGame_Map02","_IsoGame_Map03"], startSceneName="_IsoGame".
5. **MapFlowManager** — YENİ `Assets/Scripts/Systems/Map/MapFlowManager.cs`, DontDestroyOnLoad singleton: serialized MapListSO mapList (Resources.Load veya bootstrap'tan ata); int currentIndex; int mapsCleared; static bool IsMapTransition; int mapsPerRun=3. `GoToNextMap()`: IsMapTransition=true; mapsCleared++; eğer mapsCleared>=mapsPerRun → RIMA.Systems.Map.RoomLoader.RaiseDemoComplete() (run biter, victory) ELSE PickRandomNextIndex() (anti-immediate-repeat) → SceneManager.LoadScene(mapList.mapSceneNames[idx]). `ResetRun()`: currentIndex=start, mapsCleared=0. Bootstrap: RuntimeInitializeOnLoadMethod ile tek instance yarat + MapList'i Resources veya AssetDatabase'den yükle (build'de Resources gerekebilir — MapList_Act1'i Assets/Resources/ altına koy ya da Resources.Load path'i ayarla). TEK bootstrap kaynağı (duplicate guard).
6. **Room-clear → KAPIYI AÇ (victory DEĞİL, multi-map'te)** — `Assets/Scripts/Core/RoomClearVictoryTrigger.cs` davranışını uyarla: tüm mob ölünce, MapFlowManager varsa RaiseDemoComplete ÇAĞIRMA; bunun yerine sahnedeki çıkış kapısının Gate'ini AÇ (DoorNorth'taki RIMA.GateBehavior/Gate.Unlock(), `Assets/Scripts/Environment/Gate.cs`). MapFlowManager YOKsa (standalone _IsoGame) eski davranış (RaiseDemoComplete) kalsın. Böylece: temizle→kapı açılır→geç→sonraki map; victory sadece N map sonunda (adım 5).
7. **Kapıyı MapFlowManager'a bağla** — DoorNorth (RIMA.DoorTrigger, Assets/Scripts/Core/DoorTrigger.cs:101-109 obsolete RuntimeRoomManager çağırıyor=ölü). MapDoorExit YENİ küçük component yap (OnTriggerEnter2D, Player tag, Gate Unlocked ise) → MapFlowManager.Instance.GoToNextMap(); DoorNorth'a ekle. Ya da DoorTrigger.TriggerTransition'ı RuntimeRoomManager.Instance==null iken MapFlowManager.Instance?.GoToNextMap() çağıracak şekilde guard'la. Kapı sadece Gate Unlocked iken çalışsın (oda temizlenince).
8. **RunStats fix (BLOCKER)** — `Assets/Scripts/Core/RunStats.cs`: `public void StartNewRun()` (mevcut ResetRun çağırır) + `public void AdvanceRoom()` (roomReached++) ekle. OnSceneLoaded: MapFlowManager.IsMapTransition==true ise ResetRun ÇAĞIRMA → flag'i tüket (false yap) + AdvanceRoom(). Sadece gerçek yeni-run girişinde reset. Obsolete RuntimeRoomManager consult'unu (RunStats.cs ~166-169) kaldır.
9. **Yeni-run reset hook** — CharacterSelectScreen.OnStartRun (ve CharacterSelectController.ApplySelectedClass) içinde, LoadScene'den önce RunStats.Instance?.StartNewRun() + MapFlowManager.Instance?.ResetRun(). Class-carry zaten PlayerClassManager.SelectedClass ile çalışıyor (DOKUNMA).
10. **VERIFY** — dotnet/Unity compile temiz, read_console 0 error. (Play-mode door→map testini Opus yapacak.) CODEX_DONE.md'ye: yeni dosyalar, RunStats/RoomClearVictoryTrigger/door değişiklikleri, build settings, MapList asset, console durumu PASS/FAIL.

NOT: Player şu an her sahnede in-scene (HP/skill her map'te reset olur) — bu V1 için KABUL (not düş, persist'i sonraki tur). RoomLoader.TeardownCurrentRoom KULLANMA.
