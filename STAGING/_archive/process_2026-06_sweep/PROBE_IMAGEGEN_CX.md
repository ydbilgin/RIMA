ACTIVE RULES: be honest, do NOT fake. One small probe.

# PROBE — Do you (Codex, headless) have a REAL image-generation capability?

I need to know if this Codex environment can generate RASTER images from a text prompt (an actual image model / image
tool that outputs pixels) — NOT C# or Python code that procedurally draws shapes.

## Do exactly this:
1. State **YES** or **NO** clearly: do you have a tool that generates a PNG from a text prompt?
2. If **YES**: generate ONE test image — a 64x64 PNG, "cyan #00FFCC rift glyph on transparent background, pixel art" —
   and save it to `STAGING/imagegen_probe/test.png`. Report the tool name you used.
3. If **NO**: just say NO and name what image tools (if any) are available to you. Do NOT write code that fakes an image.

Write your answer to the DONE file. One short paragraph. Do not over-think.
