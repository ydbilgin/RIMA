# cx Rotation Smoke Test

Trivial task to verify cx_dispatch.py profile rotation after parser fix.

## Task
1. Echo the string `CX_ROTATION_TEST_OK`.
2. Write the literal text below to `CODEX_DONE_<profile>.md` exactly:

```
CX_ROTATION_TEST_OK
profile_used: <your profile name>
```

Do not modify any other files. Do not run any tools beyond writing the done file.
