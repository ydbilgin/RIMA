You are Codex executing a bounded RIMA task. Read CODEX.md for project rules. Stay strictly inside the allowed file list. Report in the format specified at the end.

TASK: Room Designer F1 SKELETON

RIMA root: F:/Antigravity Projeler/2d roguelite/RIMA
Working branch: master

Allowed paths:
- Assets/Editor/RoomDesigner/**
- Assets/Scripts/Runtime/Rooms/RoomBlueprint.cs (new file only — do NOT touch if exists)
- Assets/Scripts/Runtime/Rooms/RoomPrefabLink.cs (new file only — do NOT touch if exists)
- Assets/Tests/EditMode/Editor/RoomDesignerSkeletonTests.cs (new test file)
DO NOT TOUCH: Assets/Scripts/Runtime/Rooms/GateSocket.cs, MobSpawnPoint.cs, RoomMetadata.cs

GOAL: Create RimaRoomDesignerWindow — Custom EditorWindow with UI Toolkit shell + IMGUIContainer canvas + plane raycast mouse-to-grid + Save Prefab pipeline.

FILES TO CREATE:

1. Assets/Editor/RoomDesigner/RimaRoomDesignerWindow.cs (~250 LOC)
- [MenuItem("RIMA/Room Designer")] -> window open
- CreateGUI() loads UXML, exposes left-panel slot (#left-panel), right-panel slot (#right-panel), center canvas slot (#canvas-host)
- Implements IRoomDesignerContext interface (see below)
- OnEnable/OnDisable: preview camera lifecycle, scene + tilemap setup
- EditorApplication.update += PollMcp placeholder (polls a response folder every 0.5s, logs found .json file count — F3 will fill this)

2. Assets/Editor/RoomDesigner/UI/RoomDesignerWindow.uxml
Content:
<UXML xmlns:ui="UnityEngine.UIElements">
  <ui:VisualElement name="root" class="rd-root">
    <ui:VisualElement name="toolbar" class="rd-toolbar">
      <ui:Button name="btn-save" text="Save Room"/>
      <ui:Button name="btn-new"  text="New Room"/>
      <ui:DropdownField name="active-layer" label="Layer"/>
    </ui:VisualElement>
    <ui:VisualElement name="body" class="rd-body">
      <ui:VisualElement name="left-panel"  class="rd-panel rd-left"/>
      <ui:VisualElement name="canvas-host" class="rd-canvas"/>
      <ui:VisualElement name="right-panel" class="rd-panel rd-right"/>
    </ui:VisualElement>
  </ui:VisualElement>
</UXML>

3. Assets/Editor/RoomDesigner/UI/RoomDesignerWindow.uss
- USS variables: --rd-bg, --rd-panel-bg, --rd-accent (biome theming F4)
- Flexbox: toolbar top, body bottom horizontal (left-panel fixed 280px, right-panel fixed 320px, canvas flex-grow:1)

4. Assets/Editor/RoomDesigner/Canvas/RoomDesignerCanvas.cs (~250 LOC)
- IMGUIContainer wrapper class
- Camera previewCam (orthographic, layer mask = RoomDesigner only)
- Creates scene objects: GameObject "_RoomDesignerStage" (HideFlags.DontSave) + Grid + 3 Tilemaps (Floor/Walls/Decals) + camera
- DrawCanvas method using Handles.DrawCamera(r, previewCam) + HandleUtility.GUIPointToWorldRay
- Mouse hit: Plane(Vector3.forward, Vector3.zero) raycast -> grid.WorldToCell -> ctx.HoveredCell, ctx.InvokeBrush on MouseDown/MouseDrag
- ScrollWheel -> Zoom
- HiDPI: check EditorGUIUtility.pixelsPerPoint
- Repaint only on drag/zoom/pan or ctx.IsDirty
- 60fps debug gate: System.Diagnostics.Stopwatch, last 60 frames average to debug log only if ROOM_DESIGNER_DEBUG_PERF scripting define is active

5. Assets/Editor/RoomDesigner/IRoomDesignerContext.cs (~80 LOC)
CRITICAL — interface signature must not change (Palette and Brush tasks depend on it):

namespace RIMA.Editor.RoomDesigner
{
    using UnityEngine;
    using UnityEngine.Tilemaps;
    using UnityEngine.UIElements;

    public interface IRoomDesignerContext
    {
        Tilemap FloorTilemap { get; }
        Tilemap WallsTilemap { get; }
        Tilemap DecalsTilemap { get; }
        Tilemap GetActiveTilemap();
        RoomLayer ActiveLayer { get; set; }

        TileBase ActiveTile { get; set; }
        BrushMode ActiveBrush { get; set; }

        Vector3Int HoveredCell { get; set; }
        bool IsCanvasHovered { get; }

        void InvokeBrush(int mouseButton, Vector3Int cell);

        VisualElement LeftPanel { get; }
        VisualElement RightPanel { get; }

        void MarkDirty();
    }

    public enum RoomLayer { Floor, Walls, Decals }
    public enum BrushMode { Stamp, Eraser, Picker, Bucket }
}

Concrete impl: RimaRoomDesignerWindow itself implements IRoomDesignerContext.

6. Assets/Editor/RoomDesigner/SaveLoad/RoomSaver.cs (~200 LOC)
Atomic save pattern (manual rollback REQUIRED):

public static class RoomSaver
{
    public static (string prefabPath, string blueprintPath) Save(GameObject roomRoot, string roomId, string biome)
    {
        string dir = string.Format("Assets/_Generated/Rooms/{0}", biome);
        Directory.CreateDirectory(dir);
        string soPath     = string.Format("{0}/{1}.asset", dir, roomId);
        string prefabPath = string.Format("{0}/{1}.prefab", dir, roomId);
        var made = new List<string>();
        try
        {
            var bp = ScriptableObject.CreateInstance<RoomBlueprint>();
            AssetDatabase.CreateAsset(bp, soPath); made.Add(soPath);

            PrefabUtility.SaveAsPrefabAsset(roomRoot, prefabPath, out bool ok);
            if (!ok) throw new InvalidOperationException("Prefab save failed");
            made.Add(prefabPath);

            AssetDatabase.ImportAsset(soPath);
            AssetDatabase.ImportAsset(prefabPath);

            var prefab = AssetDatabase.LoadAssetAtPath<GameObject>(prefabPath);
            bp.prefab = prefab;
            bp.roomId = roomId;
            bp.biome  = biome;

            var link = prefab.GetComponent<RoomPrefabLink>();
            if (link == null) link = prefab.AddComponent<RoomPrefabLink>();
            link.blueprint = bp;

            EditorUtility.SetDirty(bp);
            EditorUtility.SetDirty(prefab);
            AssetDatabase.SaveAssets();
            return (prefabPath, soPath);
        }
        catch
        {
            foreach (var p in made) AssetDatabase.DeleteAsset(p);
            throw;
        }
    }
}

7. Assets/Scripts/Runtime/Rooms/RoomBlueprint.cs (create only if it does NOT already exist)
[CreateAssetMenu(menuName="RIMA/Room Blueprint")]
public class RoomBlueprint : ScriptableObject
{
    public string roomId;
    public string biome;
    public GameObject prefab;
    public string roomType;
    public int gateCount;
}

8. Assets/Scripts/Runtime/Rooms/RoomPrefabLink.cs (create only if it does NOT already exist)
public class RoomPrefabLink : MonoBehaviour
{
    public RoomBlueprint blueprint;
}

TEST FILE: Assets/Tests/EditMode/Editor/RoomDesignerSkeletonTests.cs
Tests:
- Window can be opened: EditorWindow.GetWindow<RimaRoomDesignerWindow>() not null, then close it
- IRoomDesignerContext default state valid: after open, ActiveBrush == BrushMode.Stamp, ActiveLayer == RoomLayer.Floor, HoveredCell == Vector3Int.zero
- RoomSaver.Save happy path: create temp GameObject, call Save with biome="_TEST" roomId="test_room", verify .prefab and .asset exist, then cleanup with AssetDatabase.DeleteAsset on both + directory
Use [Test] with NUnit. Check if Assets/Tests/EditMode/ has an .asmdef — if yes, ensure it references UnityEditor and Unity.2D.Tilemap.Editor; if no .asmdef found, create Assets/Tests/EditMode/EditModeTests.asmdef with proper Editor-only references.

SCOPE LIMITS — DO NOT IMPLEMENT in this task: Brush, Palette, Layer panel, Object library, AI bridge. Only skeleton.

CRITICAL PRE-EXISTING FILES — DO NOT TOUCH:
- Assets/Scripts/Runtime/Rooms/GateSocket.cs
- Assets/Scripts/Runtime/Rooms/MobSpawnPoint.cs
- Assets/Scripts/Runtime/Rooms/RoomMetadata.cs

ACCEPTANCE CRITERIA (verify each before commit):
[ ] RIMA -> Room Designer menu item exists
[ ] Window shows toolbar + left/center/right areas
[ ] Canvas IMGUI renders (empty Tilemap), 60fps+ idle
[ ] Mouse hover cell computed, shown in toolbar debug label "cell: (x,y)"
[ ] Save Room button triggers atomic save -> Assets/_Generated/Rooms/_TEST/test_room.prefab + .asset pair
[ ] Save fail scenario -> no orphan assets (rollback delete works)
[ ] No compile errors, clean console
[ ] IRoomDesignerContext interface complete, RimaRoomDesignerWindow implements it

TRUST LOOP (mandatory before commit):
1. After implementation: "100% confidence?"
2. If gaps -> fix -> re-evaluate
3. 100% confidence -> commit

COMMIT MESSAGE (use exactly):
feat(room-designer): F1 skeleton — EditorWindow + UI Toolkit shell + IMGUIContainer canvas + atomic save

- RimaRoomDesignerWindow + UXML/USS layout (toolbar + 3 panel slot)
- RoomDesignerCanvas (IMGUIContainer + Handles.DrawCamera + plane raycast)
- IRoomDesignerContext contract (Palette/Brush wire'lanacak)
- RoomSaver atomic save pattern (Prefab + RoomBlueprint pair, manual rollback)
- EditMode tests skeleton (window open + save happy path)

REPORT FORMAT:
STATUS: DONE / FAILED / PARTIAL
COMPLETED: <bullets>
ERRORS: <or NONE>
FILES_TOUCHED: <paths>
NEXT_SIGNAL: "<phrase>"
